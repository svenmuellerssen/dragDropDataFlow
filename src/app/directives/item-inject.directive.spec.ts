import { ItemInjectDirective } from './item-inject.directive';

describe('ItemInjectDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemInjectDirective();
    expect(directive).toBeTruthy();
  });
});
